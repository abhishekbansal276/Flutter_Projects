import 'package:flutter/material.dart';

class YourOrdersList extends StatelessWidget {
  const YourOrdersList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Your Orders Screen"),
      ),
    );
  }
}
